# Workflow Status - TicTacToe Portfolio Project
# Architecture Documentation Only (optimized for recruiter + future dev assistance)

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - skipped: Intentionally skipped workflow
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/architecture.md")

generated: "2026-01-14"
project: "tictactoe"
project_type: "Portfolio - Clean Architecture Demonstration"
selected_track: "method-architecture-only"
field_type: "brownfield"
workflow_path: "_bmad/bmm/workflows/workflow-status/paths/method-brownfield.yaml"

workflow_status:
  # ============================================
  # PHASE 0: Documentation (PREREQUISITE)
  # ============================================
  - phase: 0
    name: "Documentation"
    note: "Analyze existing codebase before documenting architecture"
    workflows:
      - id: "document-project"
        status: "required"
        agent: "analyst"
        command: "/bmad:bmm:workflows:document-project"
        output: "Comprehensive project documentation"
        purpose: "Scan and understand existing Flutter + Firebase codebase"

  # ============================================
  # PHASE 1: Analysis (SKIPPED)
  # ============================================
  - phase: 1
    name: "Analysis"
    note: "Skipped - not needed for architecture-only documentation"
    workflows:
      - id: "brainstorm-project"
        status: "skipped"
      - id: "research"
        status: "skipped"
      - id: "product-brief"
        status: "skipped"

  # ============================================
  # PHASE 2: Planning (SKIPPED)
  # ============================================
  - phase: 2
    name: "Planning"
    note: "Skipped - focusing on technical architecture only"
    workflows:
      - id: "prd"
        status: "skipped"
      - id: "create-ux-design"
        status: "skipped"

  # ============================================
  # PHASE 3: Solutioning (ARCHITECTURE ONLY)
  # ============================================
  - phase: 3
    name: "Solutioning"
    note: "Create architecture documentation for recruiter portfolio"
    workflows:
      - id: "create-architecture"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
        output: "System architecture document"
        purpose: "Document Clean Architecture implementation, tech stack, and design decisions"

      - id: "create-epics-and-stories"
        status: "skipped"
        note: "Not needed for architecture-only track"

      - id: "test-design"
        status: "skipped"

      - id: "implementation-readiness"
        status: "skipped"

  # ============================================
  # PHASE 4: Implementation (SKIPPED)
  # ============================================
  - phase: 4
    name: "Implementation"
    note: "Phase tracked separately when needed"
    workflows:
      - id: "sprint-planning"
        status: "skipped"
